const config = {
  fileSizeLimit: 5 * 1024 * 1024,  /* 5MB */
  dialogOption: {
    cancelVariant: 'outline-secondary',
    centered: true,
    okTitle: '確定',
    cancelTitle: '取消',
  },
};

export default config;
